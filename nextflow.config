manifest {
    description = 'Legacy Brentlab RNAseq pipeline'
    nextflowVersion = '>= 20.07.0'
}

executor {
    submitRateLimit = "2 sec"
}

process {
    executor = "slurm"
    memory = 5.GB
    scratch = true
    scratch = "${launchDir}/tmp"
    errorStrategy = "finish"

    withLabel: more_cpu_mem {
        cpus = 8
        memory = 10.GB
    }
}

